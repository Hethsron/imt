#include <model/pattern.hpp>

const QString Pattern::Ambiant      = "^(Ambiant(s)?)$";

const QString Pattern::Annotations  = "^(Annotation(s)?)$";

const QString Pattern::Kinect       = "^(Kinect)$";

const QString Pattern::Robots       = "^(Robot(s)?)$";

const QString Pattern::Wearables    = "^(Wearable(s)?)$";

const QString Pattern::Xsens        = "^(Xsens)$";